# Postgres on Docker (pnpm db:up) — default for the team:
# DATABASE_URL=postgresql://monodiary:monodiary@localhost:5434/monodiary
#
# Local without Docker:
# DATABASE_URL=pglite:./.data/monodiary
DATABASE_URL=postgresql://monodiary:monodiary@localhost:5434/monodiary
PORT=3000
HOST=0.0.0.0

